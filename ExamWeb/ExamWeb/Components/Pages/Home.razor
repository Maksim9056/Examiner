@page "/"
@rendermode InteractiveServer
@inject Home Homes

@if (urlAddres != null)
{
    <Authorization @urlAddres="urlAddres" />

}
@code{
    [Parameter]
    public string urlAddres { get; set; }
    protected override async Task OnInitializedAsync()
    {
        try
        {


            if (Homes == null)
            {

            }
            else
            {
                if (Homes.urlAddres != null)
                {
                    urlAddres = Homes.urlAddres;
               
                }

            }
        }
        catch(Exception)
        {
            
        }
    }
}